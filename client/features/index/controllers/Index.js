(function(){

    angular.module('tinder').controller('features.index.controllers.index', Ctrl);

    Ctrl.$inject = [
        '$scope',
        '$rootScope',
        '$state'
    ];

    function Ctrl(
        $scope,
        $rootScope,
        $state
    ) {


    }

})();
