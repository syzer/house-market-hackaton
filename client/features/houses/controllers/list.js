(function(){

    angular.module('tinder').controller('features.houses.controllers.list', Ctrl);

    Ctrl.$inject = [
        '$scope',
        '$rootScope',
        '$state'
    ];

    function Ctrl(
        $scope,
        $rootScope,
        $state
    ) {


    }

})();
